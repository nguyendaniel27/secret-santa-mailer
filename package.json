{
  "name": "secret-santa-mailer",
  "version": "1.0.0",
  "description": "Simple webserver app to record user inputs with restrictions, then match said users and send their matches to them.",
  "main": "node.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "directory-tree": "^3.2.2",
    "express": "^4.18.1",
    "readline-sync": "^1.4.10",
    "ws": "^8.6.0"
  }
}
